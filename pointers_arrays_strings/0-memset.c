#include "main.h"
/**
 *_memcpy - fills memory
 *@s: pointer
 *@b: constant
 *@n: number of bytes
 *Return: always 0
 */
char *_memcpy(char *s, char b, unsigned int n)
{
	unsigned int i;
	for (i = 0; i < n; i++)
	{	s[i] = b
		
	return (s);
	}
}
